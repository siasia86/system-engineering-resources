---
name: md-link-check
description: Markdown 링크·앵커·목차·헤딩 구조 검증 규칙. .md 파일 작성/수정 후 사용합니다. 파일 링크는 sia-md-link-check, 앵커와 헤딩 구조는 sia-md-heading-check 가 담당합니다.
---

# Markdown Link & TOC Check Rules

## 목적

`.md` 파일 작성/수정 후 링크와 헤딩 구조를 검증합니다. 검사 항목별로 담당 도구가 다릅니다.

| 검사 항목                            | 담당 도구              | 비고                        |
|--------------------------------------|------------------------|-----------------------------|
| 내부 파일 링크 (`[텍스트](경로.md)`) | `sia-md-link-check`    | `#anchor` 는 검증 대상 제외 |
| 앵커 링크 (`#섹션명`)                | `sia-md-heading-check` | `anchor` 검사               |
| H2 번호 연속성                       | `sia-md-heading-check` | `number` 검사               |
| 헤딩 레벨 건너뜀                     | `sia-md-heading-check` | `level` 검사                |
| 참조되는 앵커 중복                   | `sia-md-heading-check` | `duplicate` 검사            |
| 목차 완결성 (H2 누락 여부)           | `sia-md-heading-check` | `toc` 검사                  |

```bash
sia-md-link-check <path>      # 파일 링크
sia-md-heading-check <path>   # 앵커·번호·레벨·중복·목차
sia-md-style-check <path>     # 표 정렬·문체·푸터
```

🟡 `sia-md-link-check` 는 docstring 에 `#anchor` 링크를 검증 제외로 명시합니다. 앵커 검증은
`sia-md-heading-check` 가 담당하므로 두 도구를 함께 실행합니다.

---

## 1. 앵커 링크 검사

### GitHub 앵커 변환 규칙

GitHub 은 `github-slugger` 로 앵커를 만듭니다. 순서는 다음과 같습니다.

1. 소문자 변환
2. 구두점·기호 **제거**
3. 공백 → `-` 변환 (연속 공백도 각각 `-`로, 축약 없음)

제거되지 않고 **유지되는** 문자입니다. 이 구분을 틀리면 대량 오판이 발생합니다.

| 유지                  | 예시                                           |
|-----------------------|------------------------------------------------|
| 문자·숫자 (한글 포함) | `Ubuntu`, `설치`, `2026`                       |
| 하이픈 `-`            | `B-Tree` → `b-tree`                            |
| **밑줄 `_`**          | `TIME_WAIT` → `time_wait`, `__init__` 유지     |
| 이모지                | 경고 표기가 포함된 헤딩은 이모지가 앵커에 남음 |

제거되는 문자 예시입니다.

| 헤딩                      | 앵커                      | 이유                                  |
|---------------------------|---------------------------|---------------------------------------|
| `컨텍스트 / 도구 상세`    | `#컨텍스트--도구-상세`    | `/` 제거 → 양쪽 공백이 각각 `-`       |
| `토큰 절약 & 성능 최적화` | `#토큰-절약--성능-최적화` | `&` 제거 → 이중 하이픈                |
| `` `@` 참조 기능 ``       | `#--참조-기능`            | 백틱·`@` 제거 → 앞 공백 `-`           |
| `Plan — 에이전트`         | `#plan--에이전트`         | `—` 제거 → 이중 하이픈                |
| `CI/CD 비대화형`          | `#cicd-비대화형`          | `/` 제거, 양쪽 공백 없으면 단순 제거  |
| `B-Tree / B+Tree 구조`    | `#b-tree--btree-구조`     | `/` 와 `+` 제거                       |
| ``부정 패턴 (`!`)``       | `#부정-패턴-`             | 괄호·백틱·`!` 제거 → 말미 하이픈 잔존 |

🟡 헤딩 말미에 괄호나 기호를 두면 앵커 끝에 하이픈이 남습니다. 링크가 지저분해지므로
제목에서 장식용 기호를 빼는 편이 낫습니다.

### 앵커 검증 방법

직접 계산하지 않고 도구를 사용합니다. 앵커 규칙은 유니코드 구두점 범위가 넓어
간이 정규식으로 재현하면 어긋납니다.

```bash
sia-md-heading-check <path>
```

`sia-md-heading-check` 는 `github-slugger` 의 문자 집합을 그대로 이식하여 사용합니다.
간이 규칙(`[^\w\s-]` 등)은 이모지를 제거하고 상첨자를 남기는 등 원본과 달라집니다.

### 코드블록 내부 헤딩 주의

` ``` ` 안의 `## 헤딩`은 실제 섹션이 아닙니다.
목차 링크 대상은 **코드블록 밖의 H2만** 해당합니다.

### 중첩 코드블록은 4-backtick 필수

코드블록 예시 안에 또 코드블록을 넣을 때 외부 펜스를 3-backtick 으로 두면 안 됩니다.
CommonMark 는 정보 문자열이 없는 펜스를 닫는 펜스로 처리하므로, 내부의 bare ` ``` ` 가
외부 블록을 조기에 닫습니다. 이후 펜스 상태가 반전되어 **뒤따르는 실제 섹션들이
코드블록으로 렌더링됩니다.**

```
❌ 잘못된 중첩 — 외부가 L2 에서 닫히고, L5 가 새 블록을 열어 이후 섹션을 삼킴
   L1  ```markdown
   L2  ```
   L3  > /context
   L4  `` `
   L5  ```
   L6
   L7  ## 5. 다이어그램      <- 코드로 렌더링됨

✅ 외부를 4-backtick 으로 승격
   L1  ````markdown
   L2  ```
   L3  > /context
   L4  `` `
   L5  ````
```

판정 규칙입니다.

| 조건                               | 닫는 펜스 여부 |
|------------------------------------|----------------|
| 언어 태그 있음 (bash, json 등)     | 아님 (내용)    |
| 언어 태그 없음                     | 닫음           |
| 들여쓰기 4칸 이상                  | 아님 (내용)    |
| 여는 펜스보다 backtick 개수가 적음 | 아님 (내용)    |

🟡 이 규칙 누락으로 `STYLE.md` 자체에서 5개 섹션이 코드블록으로 렌더링되던 결함이
있었습니다(2026-08-27 수정). 중첩 예시를 쓸 때는 반드시 `sia-md-heading-check` 로
확인합니다.

---

## 2. 목차 완결성 검사

### 규칙

- 코드블록 밖의 모든 H2 섹션은 목차에 포함되어야 합니다.
- 단, 아래 고정 섹션은 목차에서 제외합니다: `목차`, `참고 자료`, `통계`, `참고 URL`
- 목차에 있는 링크가 실제 H2 섹션(코드블록 밖)을 가리키는지 확인합니다.

### 검사 절차 (문서 작성/수정 후 필수)

1. 코드블록 밖 H2 목록 추출
2. 각 H2의 올바른 앵커 계산 (`github_anchor()` 함수 사용)
3. 목차의 모든 링크 앵커와 대조 → 불일치 시 수정
4. 목차에 없는 H2 → 목차에 추가

---

## 3. 내부 파일 링크 검사

```bash
# 특정 파일의 내부 링크 검사
BASE=/path/to/repo
grep -oP '\[.*?\]\(\K[^)#]+\.md' FILE.md | while read link; do
  [ -f "$BASE/$link" ] && echo "OK  $link" || echo "❌  $link"
done
```

### 수정 규칙

1. 파일명 변경 반영 — 링크 텍스트와 경로 모두 수정
2. 경로 누락 — 서브디렉토리 경로 추가
3. 상대 경로 — 서브디렉토리에서 상위 참조 시 `../` 사용

---

## 4. H2 번호 체계

목차가 있는 문서의 H2 섹션은 번호를 부여합니다.

```markdown
# ❌ 번호 없음
## 개요
## 설치
## 사용법

# ✅ 번호 있음
## 1. 개요
## 2. 설치
## 3. 사용법
```

### 번호 제외 대상

아래 섹션은 번호를 붙이지 않습니다.

| 섹션                                                | 이유                      |
|-----------------------------------------------------|---------------------------|
| `목차`, `참고 자료`, `통계`                         | 고정 푸터 섹션            |
| `문서 목록`, `문서 트리`                            | README 인덱스 구조        |
| OS별/레이어별/연차별 분류 섹션                      | 번호보다 분류 체계가 우선 |
| 단계 번호가 이미 있는 섹션 (`1단계:`, `Phase 1` 등) | 중복 번호 방지            |

### 번호 불필요 문서 유형

아래 유형의 문서는 H2 전체에 번호를 붙이지 않습니다.

| 유형                   | 예시                                                       |
|------------------------|------------------------------------------------------------|
| OS/버전별 분류 문서    | `root_password_recovery.md` — CentOS 7, Rocky Linux 9 등   |
| 레이어/프로토콜별 분류 | `ddos_attacks_*.md` — Layer 3, Layer 4, Layer 7            |
| 섹션명에 번호 포함     | `1단계:`, `2단계:`, `Phase 1` 등이 이미 섹션명에 있는 경우 |
| 비교/분석 문서         | `*_comparison.md`, `ansible_vs_jenkins.md`                 |
| README 인덱스          | 문서 목록 나열이 주목적인 파일                             |
| 영문 기능 목록         | `vim_airline.md` 등 원문 유지 필요                         |

### 번호 연속성

H2 번호는 `1.`부터 시작하여 빠짐없이 연속되어야 합니다.
번호 제외 대상 섹션은 카운트에서 제외합니다.

### 확장 번호 표기

두 가지 확장 표기를 허용합니다. 뒤 숫자가 앞 숫자보다 크면 범위, 작거나 같으면 하위 절입니다.

| 표기            | 의미                        | 카운터 처리        | 사용 예                  |
|-----------------|-----------------------------|--------------------|--------------------------|
| `## 5-10. 제목` | 한 섹션이 규칙 5~10 을 포괄 | 끝 번호(10)로 진행 | 외부 규칙 번호 대응 문서 |
| `## 1-1. 제목`  | 직전 섹션(1)의 하위 항목    | 진행하지 않음      | 규칙 본문의 세부 항목    |

범위 표기는 `work-rules` 해설서처럼 외부 규칙 번호에 대응하는 문서에서 사용합니다.
하위 절 표기 다음에는 원래 순서가 이어집니다 (`1.` → `1-1.` → `2.`).

## 5. 헤딩 레벨과 중복

### 레벨 건너뜀 금지

헤딩 레벨은 한 단계씩만 내려갑니다. H2 다음에 바로 H4 를 쓰지 않습니다.

```markdown
# ❌ H2 -> H4
## 6. 디렉토리 구조
#### 디렉토리 역할 설명

# ✅ H2 -> H3
## 6. 디렉토리 구조
### 디렉토리 역할 설명
```

외부 프로젝트 README 원문을 보존하는 문서는 예외입니다. `.md-heading-check.toml` 의
`[[file_skip]]` 에 사유와 함께 등록합니다.

### 중복 앵커 주의

같은 제목의 헤딩이 둘 이상이면 앵커가 겹칩니다. 링크는 **첫 번째 헤딩으로만** 이동하므로
의도한 위치로 가지 않습니다.

```markdown
# ❌ H2 와 H3 가 같은 제목
## 1단계: Endpoint 생성        <- CLI 방식
### 1단계: Endpoint 생성       <- 콘솔 방식, 앵커 충돌

# ✅ 구분 표기 추가
## 1단계: Endpoint 생성
### 1단계: Endpoint 생성 (콘솔)
```

`CHANGELOG.md` 처럼 릴리스마다 `### Added` 가 반복되는 것은 정상 형식입니다.
링크가 참조하지 않는 중복은 문제가 되지 않습니다.

---

## 6. Bold 렌더링 깨짐 방지

`**...(영문)**한글` 패턴은 일부 마크다운 파서에서 닫힘 `**`를 인식하지 못해 bold가 깨집니다.

```markdown
# ❌ 깨짐
**가상 테이블(Virtual Table)**이다.
**GC(Garbage Collection)**로 인한

# ✅ 정상 — ** 뒤 공백 1칸 추가
**가상 테이블(Virtual Table)** 이다.
**GC(Garbage Collection)** 로 인한
```

규칙: `**` 닫힘 태그 바로 뒤에 한글이 오면 반드시 공백 1칸 삽입.

## 7. 문서 작성 후 체크리스트

문서를 새로 작성하거나 H2 섹션을 추가/수정/삭제한 경우 반드시 확인합니다.

```bash
sia-md-link-check <path>
sia-md-heading-check <path>
sia-md-style-check <path>
```

- [ ] `sia-md-link-check` 0건 — 내부 파일 링크
- [ ] `sia-md-heading-check` 0건 — 앵커·번호·레벨·중복
- [ ] `sia-md-style-check` 0건 — 표 정렬·문체·푸터
- [ ] 새로 추가한 H2 섹션을 목차에 추가했는가 (자동 검사 없음)
- [ ] 코드블록 안의 `## 헤딩`을 목차에 포함하지 않았는가
- [ ] 중첩 코드블록 예시의 외부 펜스를 4-backtick 으로 썼는가
- [ ] `**...(영문)**한글` 패턴이 없는가 — 있으면 `**` 뒤 공백 추가

🟡 앵커를 손으로 계산하지 않습니다. 유니코드 구두점 범위가 넓어 간이 규칙으로는
어긋납니다. 도구 출력을 근거로 삼습니다.
